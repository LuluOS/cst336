https://cst336-luluos.c9users.io/phpmyadmin/

to make a primary key:
ALTER TABLE `Department` ADD PRIMARY KEY (`DepartmentID`);

ALTER TABLE `Department`
  DROP PRIMARY KEY,
   ADD PRIMARY KEY(
     `DepartmentID`);
     
SELECT * FROM `Employee` e INNER JOIN EmployeePay ep ON e.EmployeeID = ep.EmployeeID;

SELECT e.FName, e.LName, ep.* FROM `Employee` e INNER JOIN EmployeePay ep ON e.EmployeeID = ep.EmployeeID;